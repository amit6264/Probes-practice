apiVersion: v1
kind: Service
metadata:
  name: python-probes-service
spec:
  type: LoadBalancer
  selector:
    app: python-probes-demo
  ports:
    - protocol: TCP
      port: 80          # Service port (external)
      targetPort: 8080  # Container port
